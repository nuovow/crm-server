<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 日志根目录：默认项目下logs，可用环境变量LOG_HOME覆盖 -->
    <property name="LOG_HOME" value="${LOG_HOME:-./logs}"/>
    <property name="PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 兜底root：不看profile永远挂控制台，绝不再出现No appenders吞异常 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

    <!-- 开发：mapper包DEBUG显示SQL（root已挂CONSOLE，这里只加logger） -->
    <springProfile name="local">
        <logger name="com.nuo.crmserver.mapper" level="DEBUG"/>
    </springProfile>

    <!-- 生产：FILE只在prod里定义+引用，local下不会报"not referenced"告警。
         logback的root可多次声明，appender-ref是累加的：prod最终=CONSOLE+FILE -->
    <springProfile name="prod">
        <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${LOG_HOME}/crm-server.log</file>
            <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                <fileNamePattern>${LOG_HOME}/crm-server.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
                <maxFileSize>50MB</maxFileSize>
                <maxHistory>30</maxHistory>
                <totalSizeCap>2GB</totalSizeCap>
            </rollingPolicy>
            <encoder>
                <pattern>${PATTERN}</pattern>
                <charset>UTF-8</charset>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="FILE"/>
        </root>
    </springProfile>
</configuration>
