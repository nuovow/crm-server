spring:
  application:
    name: crm-server
  datasource:
    url: ${mysql.url}
    username: ${mysql.username}
    password: ${mysql.password}
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: assign_id